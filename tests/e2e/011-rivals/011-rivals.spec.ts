import { expect, test } from '@playwright/test'; import { TestStepHelper } from '../helpers/test-step-helper'; test('Rival priority resolves deterministically', async ({ page }, testInfo) => { await page.goto('/rivals/'); const s = new TestStepHelper(page, testInfo, '011-rivals', 'rivals'); await s.gesture('run-rival-action', () => page.getByRole('link', { name: 'Run Rival action' }).click(), async () => await expect(page.getByText(/Saruman uses Stronghold/)).toBeVisible()); });
