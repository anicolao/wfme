import { expect, test } from '@playwright/test'; import { TestStepHelper } from '../helpers/test-step-helper'; test('reconnect restores a validated event stream', async ({ page }, testInfo) => { await page.goto('/reconnect/'); const s = new TestStepHelper(page, testInfo, '012-reconnect-and-conflicts', 'reconnect'); await s.gesture('restore-session', () => page.getByRole('link', { name: 'Restore session' }).click(), async () => await expect(page.getByText(/Restored 2 events/)).toBeVisible()); });
