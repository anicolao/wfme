# Seeded setup and private views

This scenario proves that one match seed deterministically produces the public Chronicle Row and private seat hands. Every gesture is followed by semantic validation and a phone/desktop screenshot.

| Gesture | Validation | Evidence |
| --- | --- | --- |
| Enter `second-age-042` | The seed field contains the exact seed. | Generated by the tracer as `setup-000-seed-<project>.png`. |
| Click **Generate setup** | Seed, five Chronicle cards, and five private hand cards appear. | Generated by the tracer as `setup-001-generate-<project>.png`. |
| Click `galadriel-seat` | The selected private seat changes while the public Row remains five cards. | Generated by the tracer as `setup-002-switch-seat-<project>.png`. |

The full strategy requires that opponent hands, Fate cards, and deck order never appear in ordinary public selectors. This first setup slice establishes the selector boundary before multiplayer storage is connected.
