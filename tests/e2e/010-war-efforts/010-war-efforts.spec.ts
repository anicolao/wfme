import { expect, test } from '@playwright/test'; import { TestStepHelper } from '../helpers/test-step-helper'; test('accept and complete a public War Effort', async ({ page }, testInfo) => { await page.goto('/efforts/'); const s = new TestStepHelper(page, testInfo, '010-war-efforts', 'efforts'); await s.gesture('accept-effort', () => page.getByRole('link', { name: 'Accept' }).first().click(), async () => await expect(page.getByText(/Active: iron-road/)).toBeVisible()); await s.gesture('complete-effort', () => page.getByRole('link', { name: 'Complete' }).click(), async () => await expect(page.getByText(/reward paid/)).toBeVisible()); });
