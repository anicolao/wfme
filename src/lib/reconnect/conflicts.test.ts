import { expect, it } from 'vitest'; import { mergeEvents, validateSequence } from './conflicts'; it('merges duplicate events and rejects sequence gaps', () => { const events = mergeEvents([{ seq: 1, type: 'a', payload: 1 }], [{ seq: 2, type: 'b', payload: 2 }]); expect(validateSequence(events)).toHaveLength(2); expect(validateSequence([{ seq: 2, type: 'b', payload: 2 }])).toHaveLength(0); });
